/**
 * Feature flags — flip to enable/disable features without deleting code.
 */

/** Show crowd estimation UI (baseCrowd × time/weather heuristic).
 *  Set false until logic is accurate enough (tends to overcount in evening hours). */
export const SHOW_CROWD = false;
